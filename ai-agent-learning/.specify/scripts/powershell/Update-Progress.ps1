#!/usr/bin/env pwsh
# Update-Progress.ps1 (deprecated)
# 项目不再维护 progress 文件，交由 AI 记忆与提示进行会话管理。

Write-Host "[DEPRECATED] Progress tracking via progress.md is removed in UbD-only flow." -ForegroundColor Yellow
Write-Host "Use outlines/lessons artifacts and /socrate.check for readiness and coverage instead." -ForegroundColor Yellow
exit 0
